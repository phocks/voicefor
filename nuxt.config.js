import theme from "./content-theme-docs";

export default theme({
  pwa: {
    icon: {
      /* icon options */
      source: "[staticDir]/icon.png"
    }
  }
  // head: {
  //   link: [{ rel: "icon", type: "image/x-icon", href: "/favicon.png" }],
  // },
});
